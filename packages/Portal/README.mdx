# @c4605/react-portal

import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks'
import * as PortalStories from './src/Portal.stories'

<Meta of={PortalStories} />

## Introduction

Strong enough [`React.createPortal`](https://reactjs.org/docs/react-dom.html#createportal) replacement. Make most `React.createPortal` use case more easier.

## Props

<ArgTypes of={PortalStories} />

## Basic Usage

<Canvas of={PortalStories.BasicUsage} />

<code>{JSON.stringify(PortalStories)}</code>
